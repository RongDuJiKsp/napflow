# 运行环境，未配置时默认 production
# NODE_ENV=production

# 代理监听端口，默认 80
# LISTEN_PORT=80

# 代理监听主机，默认 127.0.0.1
# LISTEN_HOST=127.0.0.1

# 日志级别，默认 info
# LOGGER_LEVEL=info

# 代理超时时间（毫秒），默认 15000
# PROXY_TIMEOUT_MS=15000

# Web 服务目标地址
# WEB_TARGET=example

# API 服务目标地址
# API_TARGET=example