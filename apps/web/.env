## ============================================================
## 前端环境变量配置（apps/web/.env）
## 所有 NEXT_PUBLIC_ 前缀的变量会在浏览器端可用
## 优先级：.env.{NODE_ENV}.local > .env.{NODE_ENV} > .env.local > .env
## ============================================================

## ---------- 前端 API 基础路径 ----------
## 前端发起请求时的 API 基础路径
## - 以 "/" 开头（如 /api）：前端请求 /api/xxx，由独立 proxy 服务转发到后端
## - 设为完整 URL（如 http://localhost:8848）：前端直接请求后端地址，不经过代理
NEXT_PUBLIC_API_URL=/api

## ---------- 运行环境标识 ----------
## 前端运行环境标识，由 config/env.ts 导出 isDevelopment / isProduction 判断
## 使用 $NODE_ENV 自动从 Node.js 环境变量中获取
NEXT_PUBLIC_NODE_ENV=$NODE_ENV

## ---------- 密码强度校验 ----------
## 前端密码强度校验的最小密码长度（由 app/components/_base/constants.ts 读取）
## 值为正整数字符串，默认为 "8"
# STRENGTH_PASSWORD_LENGTH=8
