## ============================================================
## 后端环境变量配置（apps/server/.env）
## 由 AppConfigService（src/apps/app-config/app-config.service.ts）通过 Zod Schema 统一解析校验
## 优先级：.env.{NODE_ENV}.local > .env.{NODE_ENV} > .env.local > .env
## ============================================================

## ---------- 服务监听配置 ----------
## 服务监听的主机名（默认: localhost）
HOST_NAME=127.0.0.1
## 服务监听的端口号（默认: 3000）
PORT=8848

## ---------- MySQL 数据库配置 ----------
## 数据库连接凭据（必填），建议放在 .env.local 中，不纳入版本控制
## 参见 .env.local 文件
# MYSQL_USERNAME=root
# MYSQL_PWD=yourpassword
## 数据库地址（host:port 格式，默认: localhost:3306）
# MYSQL_HOSTPORT=localhost:3306
## 数据库名称（默认: napflow_db，不存在时会自动创建）
# MYSQL_DATABASE=napflow_db

## ---------- Root 管理员账户配置 ----------
## 初始 Root 管理员的邮箱（必填，需符合邮箱格式）
ACC_ROOT_EMAIL=root@napflow.com
## 初始 Root 管理员的昵称（必填）
ACC_ROOT_NICKNAME=rootUser
## 初始 Root 管理员的密码（必填）
ACC_ROOT_PASSWORD=root

## ---------- Root 账户同步策略 ----------
## 设置任意值即启用：每次启动时将 Root 账户信息同步为当前 .env 中的配置值
## 未设置时：仅在 Root 账户不存在时创建，已存在则跳过
## 参见 src/apps/account/account-init.service.ts
# SYNC_ROOT_ACCOUNT_FLAG=true

## ---------- JWT 配置 ----------
## JWT 签名密钥，未设置时每次启动随机生成 32 字节 hex 字符串
## 随机生成意味着每次服务重启后所有已签发 Token 失效（用户需重新登录）
## 开发模式建议在 .env.development 中配置固定值，参见 .env.development
# JWT_SECRET_KEY=your-secret-key